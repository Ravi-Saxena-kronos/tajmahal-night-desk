[project]
name = "night-desk"
version = "1.0.0"
description = "Tajmahal Hotel night desk — AssemblyAI Voice Agent"
requires-python = ">=3.12"
dependencies = []

[tool.vercel]
entrypoint = "index:app"
